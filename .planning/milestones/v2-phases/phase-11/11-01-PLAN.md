---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp-helpers.ts
  - src/mcp.ts
  - tests/mcp-helpers.test.ts
autonomous: true
requirements:
  - MCP-10
must_haves:
  truths:
    - "ok() returns { success: true, data } for any input"
    - "ok() includes meta only when provided"
    - "fail() returns { success: false, error: { message, recovery_hint } }"
    - "fail() includes code only when provided"
    - "computeRecoveryHint() returns auth message for 401/403"
    - "computeRecoveryHint() returns not-found message for 404"
    - "computeRecoveryHint() returns generic message for other errors"
  artifacts:
    - path: "src/mcp-helpers.ts"
      provides: "Exported ok, fail, computeRecoveryHint functions"
      exports: ["ok", "fail", "computeRecoveryHint"]
    - path: "tests/mcp-helpers.test.ts"
      provides: "Unit tests for all 3 helper functions"
      min_lines: 80
    - path: "src/mcp.ts"
      provides: "Imports from mcp-helpers.js instead of defining closures"
      contains: "from './mcp-helpers.js'"
  key_links:
    - from: "src/mcp.ts"
      to: "src/mcp-helpers.ts"
      via: "ESM import of ok, fail, computeRecoveryHint"
      pattern: "import.*mcp-helpers\\.js"
    - from: "tests/mcp-helpers.test.ts"
      to: "src/mcp-helpers.ts"
      via: "direct import of tested functions"
      pattern: "from '../src/mcp-helpers\\.js'"
---

<objective>
Extract ok(), fail(), and computeRecoveryHint() from the runMcpServer() closure in mcp.ts into a standalone testable module (src/mcp-helpers.ts) and write comprehensive unit tests.

Purpose: The MCP response helpers are currently trapped inside runMcpServer() as closures, making them impossible to test in isolation. Extracting them enables direct unit testing of response shapes and recovery_hint computation per MCP-10.

Output: src/mcp-helpers.ts (exported helpers), tests/mcp-helpers.test.ts (comprehensive tests), updated src/mcp.ts (imports from new module).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/mcp.ts
@src/types.ts
@tests/log.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract response helpers to src/mcp-helpers.ts and write unit tests</name>
  <files>src/mcp-helpers.ts, tests/mcp-helpers.test.ts, src/mcp.ts</files>
  <read_first>src/mcp.ts, src/types.ts, tests/log.test.ts</read_first>
  <behavior>
    - ok({ id: '1' }) returns { success: true, data: { id: '1' } } with no meta key
    - ok({ id: '1' }, { total: 5 }) returns { success: true, data: { id: '1' }, meta: { total: 5 } }
    - ok('scalar') returns { success: true, data: 'scalar' }
    - fail('not found', 'Check the resource ID and try again.') returns { success: false, error: { message: 'not found', recovery_hint: 'Check the resource ID and try again.' } } with no code key
    - fail('denied', 'Verify JULES_API_KEY is set and valid.', 'AUTH_REQUIRED') returns { success: false, error: { message: 'denied', recovery_hint: 'Verify JULES_API_KEY is set and valid.', code: 'AUTH_REQUIRED' } }
    - computeRecoveryHint(401) returns 'Verify JULES_API_KEY is set and valid.'
    - computeRecoveryHint(403) returns 'Verify JULES_API_KEY is set and valid.'
    - computeRecoveryHint(404) returns 'Check the resource ID and try again.'
    - computeRecoveryHint(500) returns 'Check network connectivity and try again. If the problem persists, verify your API key.'
    - computeRecoveryHint(undefined) returns 'Check network connectivity and try again. If the problem persists, verify your API key.'
    - computeRecoveryHint(0) returns generic message (anything not 401/403/404)
  </behavior>
  <action>
    **Step 1: Create src/mcp-helpers.ts**

    Create a new file with these exact exported functions:

    ```typescript
    export function ok<T>(data: T, meta?: Record<string, unknown>): { success: true; data: T; meta?: Record<string, unknown> } {
      return { success: true as const, data, ...(meta ? { meta } : {}) };
    }

    export function fail(message: string, recovery_hint: string, code?: string): { success: false; error: { message: string; recovery_hint: string; code?: string } } {
      return { success: false as const, error: { message, recovery_hint, ...((code ? { code } : {})) } };
    }

    export function computeRecoveryHint(status?: number): string {
      if (status === 401 || status === 403) return 'Verify JULES_API_KEY is set and valid.';
      if (status === 404) return 'Check the resource ID and try again.';
      return 'Check network connectivity and try again. If the problem persists, verify your API key.';
    }
    ```

    **Step 2: Create tests/mcp-helpers.test.ts**

    Write unit tests following the existing log.test.ts pattern (import from vitest: describe, expect, it). No need for beforeEach/afterEach since these are pure functions. Test groups:

    - `describe('ok')`: 3 tests — data only (no meta key in output), data + meta (meta key present), scalar data
    - `describe('fail')`: 2 tests — without code (no code key in output), with code (code key present)
    - `describe('computeRecoveryHint')`: 4 tests — 401/403 → auth message, 404 → not-found message, 500 → generic message, undefined → generic message

    All test assertions use exact string matching with `toBe()` or property checks with `toHaveProperty()`.

    **Step 3: Update src/mcp.ts**

    - Add import at top: `import { ok, fail, computeRecoveryHint } from './mcp-helpers.js';`
    - Remove the `ok()` function definition (lines 81-83 of current mcp.ts)
    - Remove the `fail()` function definition (lines 85-87 of current mcp.ts)
    - Update the error wrapper (lines 54-58) to use `computeRecoveryHint(e.status)` instead of inline ternary chain:
      Replace the `const recovery_hint = ...` block with:
      `const recovery_hint = computeRecoveryHint(e.status);`
    - The `fail()` call on line 59 stays but now refers to the imported function

    **Step 4: Verify**
    Run `npm test` — all tests in mcp-helpers.test.ts must pass.
    Run `npm run typecheck` — no type errors.
    Run `npm test` — existing log.test.ts tests must still pass (no regression).
  </action>
  <verify>
    <automated>cd E:/jules-dispatch && npx vitest run tests/mcp-helpers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/mcp-helpers.ts exists and exports `ok`, `fail`, `computeRecoveryHint`
    - `grep -v '^#' src/mcp-helpers.ts | grep -c 'export function'` returns 3
    - `grep -c "from './mcp-helpers.js'" src/mcp.ts` returns 1
    - `grep -c 'function ok(' src/mcp.ts` returns 0 (removed from mcp.ts)
    - `grep -c 'function fail(' src/mcp.ts` returns 0 (removed from mcp.ts)
    - `grep -c 'computeRecoveryHint' src/mcp.ts` returns at least 1
    - `npx vitest run tests/mcp-helpers.test.ts` exits with 0
    - `npx tsc --noEmit` exits with 0
    - tests/mcp-helpers.test.ts contains at least 9 test cases (3 ok + 2 fail + 4 computeRecoveryHint)
  </acceptance_criteria>
  <done>ok/fail/computeRecoveryHint are exported from src/mcp-helpers.ts, imported by src/mcp.ts, and all 9+ unit tests pass. Existing tests unmodified and still passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool handler | Client sends untrusted tool arguments via MCP protocol |
| Error propagation | Error messages from Jules API surface in MCP error responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | mcp-helpers.ts fail() | accept | recovery_hint messages are generic guidance, not internal details; API error messages already filtered by client.ts error handling |
</threat_model>

<verification>
- `npm test` passes all tests (mcp-helpers.test.ts + log.test.ts)
- `npm run typecheck` passes
- src/mcp.ts still imports and uses ok/fail from mcp-helpers.js
- No function definitions for ok/fail remain in src/mcp.ts
</verification>

<success_criteria>
- src/mcp-helpers.ts exports ok, fail, computeRecoveryHint with identical behavior to the original closures
- tests/mcp-helpers.test.ts has 9+ passing tests covering all response shapes and recovery hint categories
- src/mcp.ts imports from mcp-helpers.js, no regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/phase-11/11-01-SUMMARY.md`
</output>
