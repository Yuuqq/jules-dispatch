---
phase: 14-deprecated-alias-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/mcp.test.ts]
autonomous: true
requirements: [MCP-11]

must_haves:
  truths:
    - "Running npm test executes tests for all 7 deprecated tool aliases"
    - "jules_dispatch_task test confirms it delegates to dispatchConsolidatedTasks and returns a single DispatchResult"
    - "jules_dispatch_batch test confirms it accepts arrays and YAML strings and returns summary + results"
    - "jules_get_session test confirms it returns session data from client.getSession"
    - "jules_list_sessions test confirms it returns sessions from client.listSessions"
    - "jules_status test confirms it returns legacy summary shape with prTitle and activities count"
    - "jules_list_activities test confirms it returns activities from client.listActivities"
    - "jules_get_plan test confirms it returns plan data from client.getLatestPlan"
    - "Error responses from deprecated aliases match the consolidated error format (isError=true, recovery_hint present)"
  artifacts:
    - path: "tests/mcp.test.ts"
      provides: "Integration tests for 7 deprecated MCP tool aliases"
      contains: "describe('jules_dispatch_task'"
  key_links:
    - from: "tests/mcp.test.ts"
      to: "src/mcp.ts"
      via: "createMcpServer import and tool calls"
      pattern: "callTool.*jules_dispatch_task|jules_dispatch_batch|jules_get_session|jules_list_sessions|jules_status|jules_list_activities|jules_get_plan"
---

<objective>
Add integration tests for all 7 deprecated MCP tool aliases to verify they delegate correctly after Phase 13 refactoring.

Purpose: MCP-11 requires test coverage proving deprecated tool aliases work. Phase 13 refactored them to thin wrappers; now we verify those wrappers produce correct results.
Output: tests/mcp.test.ts extended with ~14 new tests (2 per deprecated tool: happy path + error).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Executor needs these from existing test infrastructure and source -->

From tests/mcp.test.ts (EXISTING — reuse these helpers):
```typescript
async function createTestServer(mockClient: JulesClient): Promise<{ client: Client; server: McpServer; cleanup: () => Promise<void> }>
function createMockClient(overrides: Partial<JulesClient> = {}): JulesClient
async function callTool(client: Client, name: string, args: Record<string, unknown>): Promise<{ isError: boolean; data: any }>
```

From src/mcp.ts (tool schemas — these are the exact argument shapes):
```typescript
// jules_dispatch_task: { title: string, prompt: string, source?: string, branch?: string, autoMode?: 'AUTO_CREATE_PR'|'NONE', requirePlanApproval?: boolean }
// jules_dispatch_batch: { tasks: TaskDefinition[] | string, format?: 'yaml'|'json', parallel?: number }
// jules_get_session: { sessionId: string }
// jules_list_sessions: { pageSize?: number, pageToken?: string }
// jules_status: { sessionIds: string[] }
// jules_list_activities: { sessionId: string, pageSize?: number, pageToken?: string }
// jules_get_plan: { sessionId: string }
```

From src/mcp-helpers.ts:
```typescript
export function ok<T>(data: T): { success: true; data: T }
export function fail(message: string, recovery_hint?: string): { success: false; error: { message: string; recovery_hint?: string } }
```

Mock setup already in tests/mcp.test.ts:
- vi.mock('../src/dispatcher.js') — dispatchTaskDefinition mocked
- vi.mocked(dispatchTaskDefinition) — can be configured per test via mockResolvedValueOnce/mockRejectedValueOnce
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for deprecated dispatch tools (jules_dispatch_task, jules_dispatch_batch)</name>
  <files>tests/mcp.test.ts</files>
  <read_first>
    - tests/mcp.test.ts (full file — existing test patterns, helpers, mock setup)
    - src/mcp.ts (lines 112-164 — jules_dispatch_task and jules_dispatch_batch tool registrations and handler logic)
    - src/mcp-helpers.ts (ok/fail signatures)
  </read_first>
  <action>
Add two new describe blocks AFTER the existing `jules_interact` describe block (line 335) in tests/mcp.test.ts.

**describe('jules_dispatch_task')** — 2 tests:

1. `'dispatches a single task and returns DispatchResult'`:
   - Call `callTool(server.client, 'jules_dispatch_task', { title: 'Fix bug', prompt: 'Fix the null pointer', source: 'sources/github/o/r' })`
   - Assert: `isError` is falsy
   - Assert: `data.success` is true
   - Assert: `data.data.status` is 'dispatched'
   - Assert: `data.data.sessionId` is 'sess-1'

2. `'returns error with recovery_hint when dispatch fails'`:
   - Use `vi.mocked(dispatchTaskDefinition).mockRejectedValueOnce(Object.assign(new Error('Server Error'), { status: 500 }))`
   - Call `callTool(server.client, 'jules_dispatch_task', { title: 'T', prompt: 'P', source: 's/github/o/r' })`
   - Assert: `isError` is true
   - Assert: `data.error.recovery_hint` exists and is truthy

**describe('jules_dispatch_batch')** — 3 tests:

1. `'dispatches multiple tasks and returns summary'`:
   - Call `callTool(server.client, 'jules_dispatch_batch', { tasks: [{ title: 'T1', prompt: 'P1', source: 's' }, { title: 'T2', prompt: 'P2', source: 's' }] })`
   - Assert: `isError` is falsy
   - Assert: `data.data.summary.total` is 2
   - Assert: `data.data.summary.dispatched` is 2

2. `'parses YAML string input'`:
   - Call `callTool(server.client, 'jules_dispatch_batch', { tasks: 'title: T1\nprompt: P1\nsource: s/github/o/r\n---\ntitle: T2\nprompt: P2\nsource: s/github/o/r', format: 'yaml' })`
   - Assert: `isError` is falsy
   - Assert: `data.data.summary.total` is 2

3. `'returns error with recovery_hint when batch dispatch fails'`:
   - Use `vi.mocked(dispatchTaskDefinition).mockRejectedValueOnce(Object.assign(new Error('Forbidden'), { status: 403 }))`
   - Call `callTool(server.client, 'jules_dispatch_batch', { tasks: [{ title: 'T', prompt: 'P', source: 's' }] })`
   - Assert: `isError` is true
   - Assert: `data.error.recovery_hint` exists

Follow existing test patterns exactly: use `beforeEach`/`afterEach` with `createTestServer`/`cleanup`, use `createMockClient()`. Each describe block gets its own `server` and `mockClient` variables.
  </action>
  <verify>
    <automated>cd /d "E:\jules-dispatch" && npx vitest run tests/mcp.test.ts 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_dispatch_task'" equals 1
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_dispatch_batch'" equals 1
    - npm test passes with all new and existing tests green
    - Each describe block has 2-3 test cases (it blocks)
  </acceptance_criteria>
  <done>Tests for jules_dispatch_task (2 tests) and jules_dispatch_batch (3 tests) are added and all pass. Verify with: npx vitest run tests/mcp.test.ts</done>
</task>

<task type="auto">
  <name>Task 2: Tests for deprecated read tools (jules_get_session, jules_list_sessions, jules_status, jules_list_activities, jules_get_plan)</name>
  <files>tests/mcp.test.ts</files>
  <read_first>
    - tests/mcp.test.ts (full file — includes Task 1 additions, existing helpers)
    - src/mcp.ts (lines 168-222 — tool registrations for the 5 read tools and summarizeSessionLegacy helper at lines 359-386)
    - src/mcp-helpers.ts (ok/fail signatures)
  </read_first>
  <action>
Add five new describe blocks AFTER the Task 1 additions in tests/mcp.test.ts.

**describe('jules_get_session')** — 2 tests:

1. `'returns session details'`:
   - Call `callTool(server.client, 'jules_get_session', { sessionId: 'sess-1' })`
   - Assert: `data.data.id` is 'sess-1'
   - Assert: `data.data.title` is 'Test Session'

2. `'returns error when session not found'`:
   - Use `vi.mocked(mockClient.getSession).mockRejectedValueOnce(Object.assign(new Error('Not found'), { status: 404 }))`
   - Call `callTool(server.client, 'jules_get_session', { sessionId: 'bad-id' })`
   - Assert: `isError` is true
   - Assert: `data.error.recovery_hint` exists

**describe('jules_list_sessions')** — 1 test:

1. `'returns paginated session list'`:
   - Use `vi.mocked(mockClient.listSessions).mockResolvedValueOnce({ sessions: [{ id: 's1', title: 'S1', url: 'https://jules.google/s1' }] })`
   - Call `callTool(server.client, 'jules_list_sessions', { pageSize: 10 })`
   - Assert: `data.data.sessions` is an array
   - Assert: `data.data.sessions[0].id` is 's1'

**describe('jules_status')** — 2 tests:

1. `'returns legacy summary with prTitle and activities count'`:
   - Call `callTool(server.client, 'jules_status', { sessionIds: ['sess-1'] })`
   - Assert: `data.data.results` is an array with length 1
   - Assert: `data.data.results[0].sessionId` is 'sess-1'
   - Assert: `data.data.results[0].status` is 'running'
   - Assert: `data.data.results[0].activities` is a number (not undefined)

2. `'returns error status when session lookup fails'`:
   - Use `vi.mocked(mockClient.getSession).mockRejectedValueOnce(new Error('Not found'))`
   - Call `callTool(server.client, 'jules_status', { sessionIds: ['bad-id'] })`
   - Assert: `data.data.results[0].status` is 'error'
   - Assert: `data.data.results[0].error` is 'Not found'

**describe('jules_list_activities')** — 1 test:

1. `'returns activities for a session'`:
   - Call `callTool(server.client, 'jules_list_activities', { sessionId: 'sess-1' })`
   - Assert: `data.data.activities` is an array
   - Assert: `data.data.activities.length` is 1
   - Assert: `data.data.activities[0].id` is 'act-1'

**describe('jules_get_plan')** — 2 tests:

1. `'returns the latest plan for a session'`:
   - Call `callTool(server.client, 'jules_get_plan', { sessionId: 'sess-1' })`
   - Assert: `data.data.plan.id` is 'plan-1'
   - Assert: `data.data.plan.steps` is an array

2. `'returns null plan when getLatestPlan fails'`:
   - Use `vi.mocked(mockClient.getLatestPlan).mockRejectedValueOnce(new Error('Not found'))`
   - Call `callTool(server.client, 'jules_get_plan', { sessionId: 'sess-1' })`
   - Assert: `data.data.plan` is null

Follow existing test patterns exactly. Each describe block gets its own `beforeEach`/`afterEach` with `createTestServer`/`cleanup`. The `mockClient` and `server` variables are scoped per describe block (same pattern as existing consolidated tool tests).
  </action>
  <verify>
    <automated>cd /d "E:\jules-dispatch" && npx vitest run tests/mcp.test.ts 2>&1 | head -100</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_get_session'" equals 1
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_list_sessions'" equals 1
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_status'" equals 1
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_list_activities'" equals 1
    - grep -v '^#' tests/mcp.test.ts | grep -c "describe('jules_get_plan'" equals 1
    - npm test passes with all new and existing tests green (total: 82 existing + 5 dispatch + 14 new = ~27 deprecated alias tests)
    - npx vitest run reports all deprecated alias tests passing
  </acceptance_criteria>
  <done>Tests for jules_get_session (2 tests), jules_list_sessions (1 test), jules_status (2 tests), jules_list_activities (1 test), and jules_get_plan (2 tests) are added and all pass. Total deprecated alias test count: ~19 tests across 7 describe blocks. Verify with: npx vitest run tests/mcp.test.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-mock-boundary | Tests mock JulesClient and dispatcher; no real API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | tests/mcp.test.ts | accept | Test file only; no production surface. Tests validate existing behavior of deprecated wrappers. |
</threat_model>

<verification>
Run full test suite after both tasks complete:

```bash
cd /d "E:\jules-dispatch" && npm test
```

Expected: All tests pass (82 existing + ~19 new deprecated alias tests).

Also run typecheck to confirm no type regressions:
```bash
npm run typecheck
```
</verification>

<success_criteria>
1. `npm test` passes with zero failures
2. All 7 deprecated tool names have their own describe block in tests/mcp.test.ts
3. Each deprecated tool has at least 1 happy-path test verifying correct delegation
4. Error-prone tools (dispatch_task, dispatch_batch, get_session, status, get_plan) have error tests verifying recovery_hint presence
5. `npm run typecheck` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/phase-14/14-01-SUMMARY.md`
</output>
